# Wellness scorecard profile (subset)

When auditing wellness MCPs, weight:

- dense_series_caps
- demo-contract honesty
- privacy / mutation gates

CLI: `npx mcp-scorecard --path <repo> --min-score 90`
