# dense_series_caps check

Dense/stream/series tools must:

1. Advertise a **hard cap** (`max_points` in input schema preferred)
2. Advertise a **series contract marker** (`agent-safe-series/v1` and/or `contract_version`)

See agent-safe-series/v1 on delx-wellness hub.
